[pytest]
# logic.py has no Home Assistant dependency; importlib mode keeps pytest from
# importing the integration package (whose __init__ needs a running Core).
addopts = --import-mode=importlib -p no:cacheprovider -q
